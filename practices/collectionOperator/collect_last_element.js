'use strict';

function collect_last_element(collection) {
  //在这里写入代码
  return collection[collection.length-1];
}
  // var collection = [1, 2, 3, 4, 5];
  // console.log(collect_last_element(collection));
module.exports = collect_last_element;
